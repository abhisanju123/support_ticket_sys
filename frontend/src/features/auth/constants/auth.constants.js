export const AUTH_STORAGE_KEY = 'sts_auth';

/** Simulated auth until backend `/auth/*` endpoints exist. */
export const AUTH_PLACEHOLDER_MODE = false;

export const AUTH_PLACEHOLDER_NOTICE =
  'Sign-in is simulated until backend authentication is connected. Any valid email and password will work.';
