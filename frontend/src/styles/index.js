/**
 * Global styles entry points.
 *
 * Import order (via src/index.css):
 * 1. reset.css   — CSS reset
 * 2. global.css  — typography and app shell
 * 3. tailwind.css — Tailwind theme integration
 * 4. utilities.css — shared utility classes
 */

export {};
