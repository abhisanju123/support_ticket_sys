# Backend server origin (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000

# REST API path prefix — all endpoints are under /api
VITE_API_PATH=/api

# Request timeout in milliseconds
VITE_API_TIMEOUT_MS=30000
